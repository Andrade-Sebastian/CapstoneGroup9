import { BrainFlowClassifiers, BrainFlowMetrics, IBrainFlowModelParams, LogLevels } from './brainflow.types';
export declare class BrainFlowModelParams {
    inputParams: IBrainFlowModelParams;
    constructor(metric: BrainFlowMetrics, classifier: BrainFlowClassifiers, inputParams: Partial<IBrainFlowModelParams>);
    toJson(): string;
}
export declare class MLModel {
    private inputJson;
    private input;
    constructor(metric: BrainFlowMetrics, classifier: BrainFlowClassifiers, inputParams: Partial<IBrainFlowModelParams>);
    static getVersion(): string;
    static setLogLevel(logLevel: LogLevels): void;
    static setLogFile(file: string): void;
    static logMessage(logLevel: LogLevels, message: string): void;
    prepare(): void;
    predict(data: number[]): number[];
    release(): void;
    static releaseAll(): void;
}
